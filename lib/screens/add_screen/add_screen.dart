import 'package:flutter/material.dart';

// Add Page (Floating Button Functionality)
class AddPage extends StatelessWidget {
  const AddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Add Page",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
